"""
title: data_structure_practice
author: Anna
date: 2019-06-13 11:31
"""

import random

def shake_ball():
    input("Ask a question:")
    response = ["Yes definitely", "As I see it, yes", "Ask again later", "Cannot predict now", "Don't count on it","Very doubtful"]
    #rnd_num = random.randint(0, len(response)-1)
    #return response[rnd_num]

print(shake_ball())
